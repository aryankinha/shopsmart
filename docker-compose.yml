version: "3.9"

services:
  backend:
    build: ./server
    ports:
      - "4000:4000"
    env_file:
      - ./server/.env

  frontend:
    build: ./client
    ports:
      - "3000:3000"
    env_file:
      - ./client/.env
